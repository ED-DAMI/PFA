import 'package:flutter/material.dart';

// Couleurs (exemples, à adapter à votre AppTheme)
const kPrimaryColor = Colors.deepPurple;
const kAccentColor = Colors.amber;

// Tailles de padding et margin
const double kDefaultPadding = 16.0;
const double kSmallPadding = 8.0;

// Emojis pour les réactions
const List<String> kReactionEmojis = ['❤️', '😍', '🔥', '👍', '😢', '😮'];

// URLs (si vous avez des images statiques par défaut par exemple)
// const String kDefaultCoverArtUrl = 'https://example.com/default_cover.png';

// Textes (si vous avez des textes récurrents ou des clés de localisation)
// const String kAppName = "My Music App";

// Durations pour animations (exemples)
const Duration kShortAnimationDuration = Duration(milliseconds: 300);
const Duration kMediumAnimationDuration = Duration(milliseconds: 500);